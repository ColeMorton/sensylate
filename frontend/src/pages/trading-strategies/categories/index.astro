---
import Base from "@/layouts/Base.astro";
import { getAllTaxonomy, getTaxonomy } from "@/lib/taxonomyParser.astro";
import { humanize } from "@/lib/utils/textConverter";
import PageHeader from "@/partials/PageHeader.astro";

const TRADING_STRATEGIES_FOLDER = "trading-strategies";

const categories = await getTaxonomy(TRADING_STRATEGIES_FOLDER, "categories");
const allCategories = await getAllTaxonomy(
  TRADING_STRATEGIES_FOLDER,
  "categories",
);
---

<Base title={"Trading Strategy Categories"}>
  <PageHeader title={"Trading Strategy Categories"} />
  <section class="section">
    <div class="container text-center">
      <ul>
        {
          categories.map((category: string) => {
            const count = allCategories.filter((c) => c === category).length;
            return (
              <li class="m-3 inline-block">
                <a
                  href={`/trading-strategies/categories/${category}`}
                  class="bg-light text-text-dark dark:bg-darkmode-light dark:text-darkmode-text-dark block rounded px-4 py-2 text-xl"
                >
                  {humanize(category)}{" "}
                  <span class="bg-body dark:bg-darkmode-body ml-2 rounded px-2">
                    {count}
                  </span>
                </a>
              </li>
            );
          })
        }
      </ul>
    </div>
  </section>
</Base>
